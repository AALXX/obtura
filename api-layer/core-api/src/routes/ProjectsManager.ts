// import express from 'express';
// import { body, param } from 'express-validator';

// import ProjectsServices from '../Services/ProjectServices';

// const router = express.Router();

// router.post('/create-project', body('accessToken').not().isEmpty(), body('projectName').not().isEmpty(), AccountRegistrationServices.RegisterUserWithGoogle);

// router.get('/get-user-projects/:accessToken', param('accessToken').not().isEmpty(), AccountRegistrationServices.RegisterUserWithGoogle);
// router.put('/update-project', body('accessToken').not().isEmpty(), body('projectId').not().isEmpty(), body('projectName').not().isEmpty(), AccountRegistrationServices.RegisterUserWithGoogle);

// router.delete('/delete-project', body('accessToken').not().isEmpty(), body('projectId').not().isEmpty(), AccountRegistrationServices.RegisterUserWithGoogle);

// export = router;
